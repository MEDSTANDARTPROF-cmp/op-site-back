# Метрика — цели и сегменты

## Счётчик

obrprofi.ru → счётчик `75081295`

## OAuth-токен

`y0__xDLoZYJGIKJLSDg0KrNFqn1D...` (есть в `H:/666/TEST/Metrika/get_metrika.py` и в скриптах ОБРПРОФИ_АНАЛИТИКА)

⚠️ **Скоуп токена — только чтение** (`metrika:read`). Создавать/удалять цели через API нельзя (проверено: 403 Access Denied на POST). Управление целями — только через UI кабинета Метрики.

## Существующие цели на 2026-04-30

Полный список из API:

| ID | Название | Тип | Срабатываний/90дн | Состояние |
|---|---|---|---:|---|
| 437736660 | Заявка (shems) | url=`/spasibo.html` | 99 | работает (страница "спасибо" после отправки B24-формы) |
| 271875302 | JIVO Все обращения клиента в чат | step | 38 | работает |
| 271875579 | JIVO Только состоявшиеся диалоги | step | 0 | проверить — возможно настроен критерий не срабатывает |
| 288080794 | CRM: Заказ создан | cdp | 146 | работает (от B24) |
| 288080795 | **CRM: Заказ оплачен** | cdp | **68** | работает — реальные оплаты за 90 дней |
| 298246083 | О Открытие попап (`popup`) | action | 0 | мусор → удалить |
| 298246820 | Общее Форма отправлена (`form`) | action | 0 | мусор → удалить, заменили на `form_submit` |
| 298249288 | Общее Мессенджеры (`messangers`) | action | 0 | мусор + опечатка → удалить |
| 358145146 | Марквиз отправка (`marquiz_finish`) | action | 0 | мусор → удалить |
| 358145397 | Форма+марквиз (`forma_marquiz`) | action | 0 | мусор → удалить |
| 358145481 | Форма+Марквиз1 (`forma_marquiz1`) | action | 0 | мусор → удалить |
| 409242237 | CRM: Спам заказ | cdp | 11 | работает |
| 409242238 | CRM: Заказ отменен | cdp | 12 | работает |
| 455451076 | Клик WhatsApp (`WA`) | action | 0 | кнопок нет на сайте, оставить как зарезервированный ID |
| 455452024 | Клик «Консультация или заявка» (`zayavka`) | action | 1812 | работает (срабатывает на КЛИК открытия модалки, не на отправку) |
| 457371987 | Клик Telegram (`TG`) | action | 0 | кнопки скрыты d-none, оставить |
| 457374748 | Клик по телефону (`TL`) | action | 2 | **починено**: добавлен onclick на 8 tel-ссылок в шаблонах. После раскатки на прод цифры вырастут |

## Новые цели для кастомной формы (создавать в UI)

В кабинете Метрики → счётчик 75081295 → Цели → Добавить:

| Название | Тип | Идентификатор |
|---|---|---|
| Форма: открытие | JavaScript-событие | `form_open` |
| Форма: первый ввод | JavaScript-событие | `form_focus` |
| Форма: отправлено | JavaScript-событие | `form_submit` |
| Форма: канал — звонок | JavaScript-событие | `form_channel_phone` |
| Форма: канал — email | JavaScript-событие | `form_channel_email` |
| Форма: канал — мессенджер | JavaScript-событие | `form_channel_messenger` |
| B2B-форма: отправлено | JavaScript-событие | `form_b2b_submit` (на будущее) |

Везде оператор «совпадает». Чекбокс «Избранная цель» можно не отмечать.

## Сегменты ремаркетинга

В UI Метрики → Сегменты → Добавить:

- **«Посетители тестов»** — фильтр по `URL` содержит `/testi/`
- **«Открыли модалку, но не отправили»** — посещения, где сработала цель `form_open` но не сработала `form_submit` за визит
- **«Посетители категорий курсов»** — `URL` содержит `/povyshenie-kvalifikacii/`, `/perepodgotovka/`, `/kursyi-dpo/`, `/rabochie-spetsialnosti/`

## Фильтр бот-трафика

Также через UI Метрики:

- **«Без Сингапура»** — исключить визиты из Сингапура (3760 визитов/90д с 99% отказов = боты дата-центров). Применять во всех будущих отчётах.

API создавать сегменты не позволяет, делается руками.

## Где привязан JS

| Цель | Где вызывается |
|---|---|
| `TL` | `Navbar.tpl` (4 места), `Footer.tpl`, `contAdres.tpl`, `boxCTABanner.tpl`, `boxGuaranteePSK.tpl`, `msProduct.content.PSK.tpl` |
| `zayavka` | везде на CTA `data-bs-target="#MdGl"` (старая форма B24) |
| `form_open` | JS новой формы при `shown.bs.modal` |
| `form_focus` | JS новой формы при первом focusin |
| `form_submit` | JS новой формы после успеха AJAX |
| `form_channel_*` | JS новой формы после успеха, в зависимости от выбранного канала |
